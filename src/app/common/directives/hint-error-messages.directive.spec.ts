import { HintErrorMessagesDirective } from './hint-error-messages.directive';

describe('HintErrorMessagesDirective', () => {
  it('should create an instance', () => {
    const directive = new HintErrorMessagesDirective();
    expect(directive).toBeTruthy();
  });
});
